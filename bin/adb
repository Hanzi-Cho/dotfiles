#!/bin/bash
# WSL: proxy adb to the Windows SDK so it sees Windows-attached devices.
if [ -z "$ANDROID_HOME" ]; then
  echo "adb: ANDROID_HOME is not set (see secrets.example.sh)" >&2
  exit 1
fi
exec "$ANDROID_HOME/platform-tools/adb.exe" "$@"
